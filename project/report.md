## My notes
Implemented the task executor with permission checks, common format for command outputs and parsing them. Spent good amount of time on coming up with the common parsing format as I didn't want to do a complex parser involving large regexes or command specific parsers

## Companion notes
Based on the information provided, it seems that the task of implementing a secure CLI tool executor with the specified features has been completed. The key components, such as the tool
executor, permission checks, resource limits, and output parsing, are all present in the code.

## Redesign flag
