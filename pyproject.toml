[project]
name = "system-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "farkaskid", email = "farkaskid.inbox@gmail.com" }]
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]

[project.scripts]
system-agent = "system_agent:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
